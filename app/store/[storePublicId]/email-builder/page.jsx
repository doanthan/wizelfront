import EmailBuilder from "./EmailBuilder";

export const metadata = {
  title: "Aurora Studio Email Builder",
  description: "Design production-ready modular emails with an AI-assisted builder."
};

export default function EmailBuilderPage() {
  return <EmailBuilder />;
}
